<%= form_for(@role_practice) do |f| %>
  <% if @role_practice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_practice.errors.count, "error") %> prohibited this role_practice from being saved:</h2>

      <ul>
      <% @role_practice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rid %><br>
    <%= f.text_field :rid %>
  </div>
  <div class="field">
    <%= f.label :pid %><br>
    <%= f.text_field :pid %>
  </div>
  <div class="field">
    <%= f.label :role_name %><br>
    <%= f.text_field :role_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
